<% provide(:title, "Logga in") %>
<h1>Logga in</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:login, url: login_path) do |f| %>

      <%= f.email_field :username, class: 'form-control', :placeholder => "E-post" %>

      <%= f.password_field :password, class: 'form-control', :placeholder => "Lösenord" %>

      <%= f.submit "Logga in", class: "btn btn-primary" %>
    <% end %>

    <p>Har du inget konto? <%= link_to "Registrera nytt konto", signup_path %></p>
  </div>
</div>